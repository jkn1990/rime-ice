patch:
# Rime schema
# encoding: utf-8

  # 改为挂载刚创建的 custom_melt_eng.dict.yaml
  translator/dictionary: custom_melt_eng
  speller/algebra:
    # 修改为小鹤双拼的拼写派生规则，因为不在同一个文件了，前面要加上文件名
    __include: melt_eng.schema.yaml:/algebra_flypy
